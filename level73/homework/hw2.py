def sum_str(a, b):
    a = int(a) if a else 0
    b = int(b) if b else 0
    return str(a + b)
def sum_str(a, b):
    a = int(a) if a else 0  # Convert to integer, default to 0 if empty
    b = int(b) if b else 0  # Convert to integer, default to 0 if empty
    return str(a + b)  # Convert the sum back to a string
